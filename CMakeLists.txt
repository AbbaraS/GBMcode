set(HEADERS "GBM.h")
set(SOURCES "GBM.cpp")

file(GLOB MY_FILES "data/*.txt")
file(COPY ${MY_FILES} DESTINATION ${CMAKE_CURRENT_BINARY_DIR})

add_executable (GBM ${HEADERS} ${SOURCES})

if (CMAKE_VERSION VERSION_GREATER 3.12)
  set_property(TARGET GBM PROPERTY CXX_STANDARD 20)
endif()
